{
  "name": "infrastructure",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf ./build",
    "cdk": "$(yarn bin cdk)",
    "test": "NODE_OPTIONS=--experimental-vm-modules $(yarn bin jest)"
  },
  "dependencies": {
    "aws-cdk": "^2.177.0",
    "aws-cdk-lib": "^2.177.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "esbuild": "0",
    "typescript-core": "0.1.0"
  }
}
